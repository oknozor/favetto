# examples/supervisor/implement — second step of the supervisor-driven demo.
# The reference supervisor spawns this task into the plan root once the plan
# succeeds; there is no catalog edge between the two tasks.
agent = "opencode"
---

You are the implementation step of the `examples/supervisor` demo pipeline.

Read the plan at `.favetto/examples/supervisor/plan.md`, make the change it
describes in the working tree, and run the project's tests and checks. Do not
push or open a pull request from this example.

When you are done, reply with a summary of the files changed and the commands
you ran.
