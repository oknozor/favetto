# examples/hello — the minimal task behind the Catalog, Agent, Tasks and
# Events screenshots. No vars, so `Enter` in the Catalog starts it immediately.
agent = "pi"
---

Create a file named `hello.txt` containing the single line
`hello from favetto`, then stop.
