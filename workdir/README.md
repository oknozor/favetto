# workdir

Sample workspace consumed by the `mcp-filesystem` MCP server during the M2
end-to-end demo (`favetto task run summarize_workspace`).

The mock agent lists this directory, reads this file, and writes `SUMMARY.md`.
